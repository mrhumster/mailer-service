apiVersion: v1
kind: Service
metadata:
  name: mailer-service
  namespace: go-app
  labels:
    app: mailer-service
spec:
  selector:
    app: mailer-service
  ports:
    - name: metrics
      port: 9090
      targetPort: metrics